# Copy to .env and set values. Used by docker-compose (volume MOUNT_PATH:/drive).
# Control Panel에서 Save 시 이 파일의 MOUNT_PATH가 자동으로 갱신됩니다 (프로젝트 루트가 /project로 마운트된 경우).

# File Station "My Drive"에 보일 호스트 경로. 처음 설치 시 기본값.
MOUNT_PATH=/data

# TrueNAS (Control Panel → Health 탭 풀/디스크 표시)
# TRUENAS_URL=https://truenas.example.com
# TRUENAS_API_KEY=your-api-key

# AI Assistant (Gemini)
# GEMINI_API_KEY=your-gemini-api-key

# Optional
# NETDATA_URL=http://host.docker.internal:19999
# DATA_PATH=/data
